# Copia este fichero a .env (está en .gitignore) en esta misma carpeta.
# Sin estas variables, ADK intenta usar la API de AI Studio y falla con
# "No API key was provided" en cada mensaje.
GOOGLE_CLOUD_PROJECT=abahanaweb
GOOGLE_CLOUD_LOCATION=europe-west1
GOOGLE_GENAI_USE_VERTEXAI=true
